# Optional: use Hugging Face Inference API instead of loading locally
HF_TOKEN=

# Required only when you pass --send (real email applications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
